version: '3'
services:
  server:
    environment:
      - BOT_TOKEN
      - VERIFICATION
    build:
      context: .
      args:
       - "timezone=Europe/Prague"
    image: "kiwicom/iwantbot"
    volumes:
     - .:/app
    ports:
     - "8080:80"
